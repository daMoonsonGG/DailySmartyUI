import React, { Component } from "react";

export default class Searchbar extends Component {
  render() {
    return (
      <form>
        <input placeholder="Search on DailySmarty" />
      </form>
    );
  }
}
